# Completion Ledger

| ID | Remaining work | Why it matters | Status | Verification |
| --- | --- | --- | --- | --- |
| Q-5 | Execute the recorder matrix on native Windows Server 2025 and Ubuntu 24.04, plus a real WSL runner. | Cross-platform paths, locking, subprocess launch, state-directory selection, Windows handle-relative publication, and WSL/Linux store separation are implemented and simulated, but native execution is the remaining proof. | Pending | Native macOS recorder and repository gates pass. The checked-in CI jobs provide Windows, Ubuntu, and macOS coverage, with WSL gated by its self-hosted runner; record those native results when a pushed revision and runners are available. |
| Q-7 | Restart both configured Codex runtimes, review and persist trust for the managed hooks through `/hooks`, and exercise one fresh task in each. | Both homes are configured with canonical policy and skill symlinks, and fresh read-only CLI sessions emitted provider-native usage, but no Codex hook event was observed; without trusted hooks, request receipt, task correlation, and the declaration launcher remain unavailable. | Blocked — until the user reviews hook trust in both Codex runtimes | After restart, confirm current policy and skills in each runtime and observe `SessionStart` plus `UserPromptSubmit`-correlated recorder events from the main and Parall Codex homes. |
