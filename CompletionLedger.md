# Completion Ledger

| ID | Remaining work | Why it matters | Status | Verification |
| --- | --- | --- | --- | --- |
| Q-5 | Execute the recorder matrix on native Windows Server 2025 and Ubuntu 24.04, plus a real WSL runner. | Cross-platform paths, locking, subprocess launch, state-directory selection, Windows handle-relative publication, and WSL/Linux store separation are implemented and simulated, but native execution is the remaining proof. | Pending | Native macOS recorder and repository gates pass. The checked-in CI jobs provide Windows, Ubuntu, and macOS coverage, with WSL gated by its self-hosted runner; record those native results when a pushed revision and runners are available. |
| Q-7 | Finish per-home activation evidence for main Codex and Parall Codex after the target-aware recorder upgrade. Claude activation is outside this workflow by the user's explicit direction. | Recorder `0.2.2` supplied only family-level evidence, which could not distinguish both simultaneously running Codex homes. Recorder `0.2.4` and the concurrent watch remove the serial-isolation requirement, but the fresh per-home evidence depends on the pending deferred installation and reopen boundary. | Pending — deferred upgrade and one concurrent watch | Read the deferred installation receipt after reopen, review the changed Codex hooks once, then start the watch and exercise one fresh task in each Codex instance; require a saved `active=2/2` target-attributed receipt. |
| Q-8 | Deploy the implemented recorder-owned opaque target attribution and software-owned concurrent activation watch. | The user rejected quitting clients for serial proof, manually capturing sequence numbers, and repeating Terminal commands. Recorder `0.2.4`/schema `1.2`, deterministic per-home hook references, exact correlation, private reports, and bounded receipts pass the complete macOS repository gate. Fresh plan `81405bf145018e804c1dab50461c4144` with digest `2134d685f4e211c811e9019219d4e541916289ff43b5278825dcad4fe9c13955` preserves the credential, loopback port, and all three managed targets; it leaves both Codex OTel configs unchanged and changes only the recorder/runtime, stable launcher, one-time Codex hook migration, and retained Claude handler. Only Parall Codex was live when deferred job `c3f20c69fa8e31f475e8d8899e41543f` was armed against its exact host and engine process identities; main Codex and Claude need no pre-apply exit. | Pending — armed deferred Parall Codex boundary and activation proof | Close and reopen the Parall Codex host once, require the saved `deferred-install-result.json` receipt to prove a healthy installation, review the changed Codex hook, then run the concurrent watch and require its saved `active=2/2` receipt. Do not claim or fold in Claude activation. Native Windows/Linux/WSL execution remains tracked separately in Q-5. |
| Q-9 | Deploy the no-Terminal install path with a recorder-owned portable deferred handoff and minimized compatible-upgrade runtime rewrites. | The first live handoff plan expired without mutation after its hidden 900-second wait and before the next resume. A later host reboot changed volatile filesystem device identity while preserving inode and mode, so the public status command failed closed before reporting the already-saved strict `expired` receipt. The delayed-return, failure-only recovery, nonblocking artifact guard, real FIFO must-catch fixture, focused suite, complete recorder/skill suites, live expired-receipt regression, and complete six-skill repository matrix pass. Fresh exact plan `81405bf145018e804c1dab50461c4144` is reviewed without credential or port rotation; deferred job `c3f20c69fa8e31f475e8d8899e41543f` is armed with the 86,400-second default against the two exact live Parall Codex process identities. | Pending — reopened status proof | Let the user close and reopen the Parall Codex host once without Terminal, then require its healthy saved `deferred-install-result.json` receipt. Keep apply, success, rollback, trust, and activation gates strict. Native Windows/Linux/WSL execution remains tracked separately in Q-5. |
