# D-20260714-03 — Decision history is a compact direction index

Index: [DecisionHistory.md](../DecisionHistory.md)

## Detail

Request: Make `DecisionHistory.md` very concise. Keep only the decision and why
in routine context; when options existed, preserve the alternatives, selection
reason, and observed failure of previously tried approaches. Move all extended
detail to one file per decision so agents do not repeat forgotten option loops.

Direction clarification: The compact history must also reveal where the project
is heading and the unspoken intent behind user choices, including quality bar,
workflow preferences, UI hierarchy, and taste. Confirmed choices and inferred
patterns remain distinct and evidence-linked.

Options considered:

- Keep the existing single verbose file: rejected because 41 entries occupied
  11,491 routine-context words and mixed rationale with implementation/results.
- Delete extended history: rejected because earlier failed options and evidence
  would disappear, recreating the option-loop risk.
- Use a compact index with cold per-decision detail files: selected because it
  preserves the anti-loop rationale and direction while loading extended context
  only when the relevant decision is applied or challenged.

Implementation record: The 41 prior sections were split losslessly into 41
detail files, reread before the index was replaced, and summarized into stable
linked entries. A structural checker enforces the direction paragraph, two-field
entry shape, option/prior-attempt/intent/revisit clauses, stable IDs, confined
one-to-one regular detail files, backlinks, and no orphans. Linked cold history
is classified as governance and exempted from current dependency checks only by
the exact links in the validated index.

Verification: The semantic policy suite, compact-history recall and precision
suite, 42-entry index/detail integrity check, open-ledger check, exact linked
history boundary suite, public-artifact guard, and journey-governance classifier
all pass. The complete repository matrix passes all five canonical skill suites,
Python compilation, and all five standalone-copy suites.
