# D-20260820-04 — Token-usage recording is retired

Index: [DecisionHistory.md](../DecisionHistory.md)

## Detail

The user withdrew the delivery-efficiency skill and hook approach after the
reviewed `0.2.9` upgrade failed closed during the normal UI-only handoff. The
user confirmed that Codex had been fully quit with `Cmd+Q`; no SSH or manual
server-side shutdown is part of the supported retirement.

The live `holygloryTT` integration was retired with installer plan
`c8020daaa8a8f3991143869b506f6bac` and digest
`043e9dfb9f19663ebf6f99bdc52cd795deddb956ca311f6cc97889495e8efbfa`.
Apply and verify both reported zero managed targets and preserved unrelated
Codex configuration. The only installed skill entry was the direct link
`/home/holygloryTT/.codex/skills/install-delivery-efficiency-hooks` to
`/home/holyskills/skills/install-delivery-efficiency-hooks`; it was removed
after all four explicit Codex skill roots were inventoried. The other three
roots had no such entry.

Repository retirement removes the canonical skill and recorder runtime,
current AGENTS policy, documentation, validation, CI jobs, and active
user-issue rules that existed only for the withdrawn feature. Historical
decision details remain cold project history and the database permanently
retains superseded completion-issue events. Installer-retained private state
and rollback artifacts are not linked from Codex and do not reactivate hooks or
telemetry. After verification, the remaining zero-target receiver accepted its
authenticated self-retirement request and exited. The already-running Codex
process still held the pre-retirement hook list in memory and relaunched it,
making one normal UI restart the final activation boundary. No PID signal or
user-run server command is used; after restart the same authenticated endpoint
retires the idle receiver permanently.
