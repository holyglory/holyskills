# User Issue Ledger: Automation / product delivery monitoring

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-AUTOMATION-PRODUCT-DELIVERY-MONITORING-001 | Ongoing monitoring established by `coordinate-product-delivery` | The coordinator sent a few executor messages, relied on active-turn waits, and ended its run while describing coordination as ongoing because scheduled-task support was treated as optional. Durable database state was mistaken for durable execution. | Ongoing monitoring is not armed until the coordinator creates and enables a scheduled task inside the same coordinator chat, binds the executor thread and host, stores cadence/cursor/overlap/stop state, and verifies the first scheduled run. If scheduling capability is unavailable or any gate fails, report monitoring as unarmed and blocked, record the blocker, and never claim ongoing coordination or silently downgrade to active-turn polling. | Require the skill contract and deterministic fixtures to reject optional scheduling, independent-run scheduling, missing executor identity, missing retained cursor, overlapping runs, absent first-run proof, missing local app/machine availability, and missing terminal stop behavior. Preserve passing fixtures for one same-chat hourly schedule with idempotent reconciliation and stop on release completion, pause, or cancellation. |
