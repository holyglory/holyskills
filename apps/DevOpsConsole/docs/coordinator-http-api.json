{
  "schema_version": 2,
  "transport": {
    "default_url": "http://127.0.0.1:29876",
    "bind_policy": "localhost or explicit IPv4 loopback only",
    "health": "GET /healthz is anonymous and returns service/version only",
    "body_limit_bytes": 65536,
    "content_type": "application/json",
    "readiness": "api serve prints one JSON line with the actual bound port and token_file; --port 0 is supported"
  },
  "authentication": {
    "protected_routes": "/v1/*",
    "header": "Authorization: Bearer <contents of token file>",
    "default_token_file": "<coordinator-home>/api-token",
    "override": "CODEX_AGENT_COORDINATOR_TOKEN_FILE or api serve --token-file",
    "file_policy": "regular non-symlink file, mode 0600, bounded size, minimum token length 32",
    "browser_policy": "the Console reads the token server-side; it must never enter browser JavaScript, URLs, logs, screenshots, or Git"
  },
  "endpoints": {
    "GET": [
      "/healthz",
      "/v1/inventory",
      "/v1/state",
      "/v1/ports",
      "/v1/ports/assignments",
      "/v1/servers"
    ],
    "POST": [
      "/v1/ports/lease",
      "/v1/ports/release",
      "/v1/ports/assign",
      "/v1/ports/unassign",
      "/v1/servers/start",
      "/v1/servers/register",
      "/v1/servers/stop",
      "/v1/servers/restart",
      "/v1/servers/status",
      "/v1/servers/logs",
      "/v1/projects/status",
      "/v1/projects/start",
      "/v1/projects/restart",
      "/v1/projects/stop",
      "/v1/docker/ps",
      "/v1/docker/stats",
      "/v1/docker/compose-up",
      "/v1/docker/compose-down",
      "/v1/docker/logs",
      "/v1/docker/register",
      "/v1/docker/start",
      "/v1/docker/stop",
      "/v1/docker/restart"
    ]
  },
  "request_rules": {
    "mutations": "agent and canonical project attribution are required",
    "port_release": "lease_id or port plus acting agent and owning project",
    "server_launch": "structured argv is preferred; legacy cmd is parsed without a shell and rejects shell operators",
    "exact_lease_start": "lease_id requires structured argv and an active unbound manual lease owned by the same agent/project",
    "docker": "real commands resolve an absolute Docker entry point and are bounded by observation/lifecycle timeouts"
  },
  "state": {
    "version": 2,
    "keys": [
      "revision",
      "leases",
      "servers",
      "port_assignments",
      "operations",
      "history",
      "docker"
    ],
    "persistence": "private 0700 home; 0600 state/lock/token/log files; fsync plus atomic replace",
    "durable_ports": "successful server start/register pins project+name to a port until explicit unassign or attributed state reset",
    "operations": "short reservation/commit lock phases with same-target exclusion and bounded journal evidence"
  },
  "inventory_contract": {
    "project_usage": "rows include stable usage_key plus authoritative server_ids and container_names",
    "docker_ownership": "Compose labels and verified coordinator sidecars are mutation authority; name similarity is read-only evidence",
    "project_reports": "ok=false must be treated as failure even when HTTP status is 200; inspect partial, preflight_failed, classification, actions, and action_errors"
  },
  "errors": {
    "401": "missing or incorrect bearer credential",
    "400": "invalid request, ownership mismatch, capability failure, or refused mutation",
    "413": "request body exceeds 64 KiB",
    "415": "POST content type is not application/json",
    "500": "defensive read-side failure"
  }
}
