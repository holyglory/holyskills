interface:
  display_name: "Formal Web UI Verification"
  short_description: "Catch clipped, hidden, and overlapping web UI"
  default_prompt: "Use $formal-web-ui-verification to run deterministic DOM/layout checks across required desktop and mobile routes. Keep its software-owned artifact-first default: return only the bounded receipt and artifact index, never opt into human-readable report stdout."
