interface:
  display_name: "Formal Web UI Verification"
  short_description: "Verify complete UI journeys quickly and truthfully"
  default_prompt: "Use $formal-web-ui-verification with complete journey, ownership, readiness, execution-safety, theme, and UI-input mappings; continue every safe check, treat subsets/cache as development-only, then review only queued screenshot pairs and finalize their hash-bound manual-review manifest."
