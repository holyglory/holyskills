interface:
  display_name: "Formal Web UI Verification"
  short_description: "Verify journey priority, continuation, theme, and layout"
  default_prompt: "Use $formal-web-ui-verification with complete per-target journey, region, theme, continuation, and UI-input mappings; finish automatic checks first, then review only queued initial/full-page screenshot pairs and finalize their hash-bound manual-review manifest."
