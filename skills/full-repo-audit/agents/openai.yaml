interface:
  display_name: "Full Repo Audit"
  short_description: "Audit semantic implementation across entire repositories"
  default_prompt: "Use $full-repo-audit for a manifest-verified repository audit, dispatching Codex workers with fork_turns=none and Light/runtime-low effort, keeping complete worker and lead results in cold audit artifacts, and returning only a compact outcome and artifact index."
policy:
  allow_implicit_invocation: false
