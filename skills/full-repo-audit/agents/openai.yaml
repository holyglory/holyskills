interface:
  display_name: "Full Repo Audit"
  short_description: "Audit repos with attested source and visual evidence"
  default_prompt: "Use $full-repo-audit for a read-only manifest-verified repo audit. Label effort as runtime-attested only with immutable provenance, otherwise ledger-recorded-unverified; directly recheck every high-risk file; dispatch deterministic source and journey batches; bind real hashed screenshot/native/formal-verifier evidence; reconcile implementation, UI, journey, and test gaps; then return a prioritized plan."
