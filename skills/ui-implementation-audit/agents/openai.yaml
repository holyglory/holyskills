interface:
  display_name: "UI Implementation Audit"
  short_description: "Audit an existing implemented UI end to end"
  default_prompt: "Use $ui-implementation-audit only after confirming substantive repo-owned UI; run source/formal/automatic checks first, then review only changed-input visual-queue screenshot pairs and retain the formal report, queue, manual-review manifest, and source evidence in cold artifacts."
policy:
  allow_implicit_invocation: false
